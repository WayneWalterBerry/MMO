# Orchestration Log: Bart (Parser Pipeline Build)

**Spawn Time:** 2026-03-19T17-22-26Z  
**Agent:** Bart (claude-sonnet-4.5, background mode)  
**Task:** Phase 1 + 2 Python build pipeline for embedding parser

## Outcome: SUCCESS

**Deliverables Created:**
1. `scripts/generate_parser_data.py` — Training data extraction (local mode verified: 54 verbs, 39 objects, 29,582 pairs)
2. `scripts/build_embedding_index.py` — GTE-tiny ONNX embedding encoder
3. `scripts/requirements.txt` — Python dependencies
4. `data/parser/.gitkeep` — Output directory marker
5. `src/assets/parser/.gitkeep` — Output directory marker

**Key Technical Decisions:**
- Two-mode design: local (zero deps, deterministic) + LLM (GPT-4, higher quality)
- CSV intermediate format decouples Phase 1 and Phase 2
- Regex extraction of verb/object metadata (no Lua parser needed)
- All 54 verbs covered: 31 primary handlers + 23 aliases
- Model caching in `models/` (gitignored, CI/CD cache-ready)
- Dual output: uncompressed JSON (debug) + gzip (production)

**Files Modified:**
- `.gitignore` — Added `models/`, `__pycache__/`, `*.pyc`

**Verification:**
- Phase 1 local mode tested: 29,582 training pairs generated
- Verb extraction regex validated against verbs/init.lua handlers
- Object extraction regex validated against all .lua files in src/meta/objects/

**Impact:**
- Tier 2 embedding matcher now has proven data pipeline
- No external deps in local mode (CI/CD portable)
- Ready for Phase 3 runtime integration and Phase 5 CI/CD automation

**Decision Filed:** `.squad/decisions/inbox/bart-parser-pipeline.md`

# Orchestration Log: gil-review

**Timestamp:** 2026-03-28T22:25:46Z  
**Agent:** Gil (CI/Build Engineer)  
**Task:** Review CI integration for mutation-graph-linter  
**Mode:** background (claude-haiku-4.5)

## Outcome

⚠️ **CI gaps identified. Infrastructure changes needed.**

### Findings

**CI Gaps (3):**
1. **Python 3.9+ missing** — GitHub Actions runners default to Python 3.8 or 3.10 (no 3.9). `mutation-lint.ps1` will fail. Need: `actions/setup-python@v4` with `python-version: '3.9'`
2. **No lint step in CI** — `.github/workflows/` has no linting phase. New linter needs to be gated before tests or as separate step
3. **PowerShell 7 fallback** — GitHub Actions uses PS7 on Windows, but `mutation-lint.ps1` written for PS5+. Needs compatibility check or PSVersion guards

**Environment Concerns:**
- Edge extractor (`scripts/mutation-edge-check.lua`) requires working Lua interpreter — already available in test step
- Python dependencies (`pytest` for tests only, not runtime) — need `pip install` in WAVE-1 test phase
- No Python path validation on Windows — may fail if PATH doesn't include Python

**Recommendations:**
1. Add `setup-python` action before linter step
2. Add `-Version 7.0` guard in `.ps1` or use pwsh binary instead of powershell
3. Add Lua + Python version checks to `run-before-deploy.ps1`

### Impact

Linter cannot run in CI until these 3 gaps are closed. WAVE-0 pre-flight must include CI setup.

---

*— Scribe, 2026-03-28T22:25:46Z*

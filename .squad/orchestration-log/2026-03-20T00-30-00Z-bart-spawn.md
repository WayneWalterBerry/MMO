# Orchestration Log: Bart (FSM Inline Refactor)
**Spawn Date:** 2026-03-20T00:30:00Z  
**Mode:** Background (claude-sonnet-4.5)  
**Status:** ✅ Completed

## Manifest
1. FSM-inline refactor: Embed all FSM definitions into object files
2. Create 4 new FSM objects: candle, poison-bottle, vanity, curtains
3. Merge match + nightstand FSMs from `src/meta/fsms/` into object files
4. Delete `src/meta/fsms/` directory entirely
5. Update FSM engine to read `obj.states` directly

## Deliverables
- ✅ Merged match FSM into `objects/match.lua` (3 states: unlit, lit, spent)
- ✅ Merged nightstand FSM into `objects/nightstand.lua` (2 states: closed, open)
- ✅ Created `objects/candle.lua` (4 states: unlit, lit, stub, spent; 100 + 20 turn durations)
- ✅ Created `objects/poison-bottle.lua` (3 states: sealed, open, empty; DRINK/POUR verbs)
- ✅ Created `objects/vanity.lua` (4 states: intact, cracked, broken; mirror break mutation)
- ✅ Created `objects/curtains.lua` (2 states: closed, open; light-blocking property)
- ✅ Deleted `src/meta/fsms/` directory and 7 state definition files
- ✅ Updated FSM engine to detect `obj.states` instead of `obj._fsm_id`
- ✅ All verb handlers updated to check FSM state directly

## Hybrid Model Implemented
Objects now support BOTH FSM (reversible state changes) AND mutations (destructive transformations):
- Vanity: FSM for intact/cracked/broken + mutation for mirror shatter
- Curtains: FSM for open/close + potential tear mutation
- Poison-bottle: FSM for sealed/open/empty + DRINK effect side-by-side

## Tests Passed
- All existing tests pass with FSM-inline model
- FSM transitions work as before
- Backward compatibility: plain objects without `.states` unaffected

## Key Decision: FSM Transition Aliases
Added support for `aliases` field on transitions. Verb handlers check aliases when deciding verb delegation.
Example: match strike transition has `aliases = {"light", "ignite"}`. Verb handler for "light" checks if transition matches and delegates to strike handler.

## Git Commit
- Message: "FSM-inline refactor: embed all definitions, delete src/meta/fsms/, add candle/poison-bottle/vanity/curtains"
- Files changed: 12 objects created/modified, src/engine/fsm/init.lua updated, 7 state files deleted

## Cross-Agent Impact
- **Nelson:** FSM objects now inline; test against live object files, not separate state files
- **CBG:** FSM design pattern validated; container model integrated with wearable system
- **Brockman:** Newspaper headers added; morning/evening edition labels live

## Notes
- One file = one object = one FSM (no scatter)
- Single source of truth per object
- FSM engine unchanged in purpose, only location changed

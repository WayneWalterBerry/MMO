# Bart Spawn — 2026-03-24T16-40-00Z

**Task:** P0-B Lark grammar prototype + existing validation audit  
**Mode:** background  
**Status:** SUCCESS

## Outcome

### Lark Grammar Prototype

**Result:** Lark-based Lua object parser successfully parses all 83 objects in `src/meta/objects/` with zero failures.

**Implementation:** Three-phase pipeline
1. **Tokenize** — Custom Lua tokenizer handles strings, comments, keywords, nesting
2. **Preprocess** — Strip preamble (local declarations), neutralize function bodies → `__FUNC__` placeholders
3. **Lark Parse** — Earley grammar: `return { key = value, ... }` with nested tables, arrays, booleans, nil, trailing commas

**Key Findings:**
- 82/83 objects are pure data tables with literals, tables, and function expressions
- 1/83 objects (wall-clock.lua) uses programmatic generation with `for` loops and variable references
- 50%+ of objects have functions (on_look, on_feel, factory, guard) — safely handled by preprocessing
- Nightstand pattern (local function preamble) correctly handled by block-depth tracker

**Applicable to:** Room files follow the same pattern and should parse with the same grammar.

### Validation Audit

**Result:** Loader performs 3 checks; 22 gaps identified in template validation.

**Gaps Identified:**
- Missing schema enforcement for object templates
- No validation of sensory fields (on_feel required by D-0.5)
- No checks for material consistency (D-9)
- No verification of containment hierarchy depth
- No FSM transition validation

## Decision Inbox

File: `bart-lark-grammar.md` (merged to decisions.md)

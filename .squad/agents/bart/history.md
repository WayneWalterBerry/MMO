# Bart — History (Summarized)

## Project Context

- **Project:** MMO — A text adventure MMO with multiverse architecture
- **Owner:** Wayne "Effe" Berry
- **Stack:** Lua (engine + meta-code), cloud persistence
- **Key Concepts:** Each player gets their own universe instance with self-modifying meta-code. Objects are rewritten (not flagged) when state changes. Code IS the state.

## Core Context (Summarized)

**Role:** Architect — engine design, verb systems, FSM mechanics, mutation patterns, puzzle systems

**Major Systems Built:**
- **Engine Foundation:** Loader (sandboxed execution), Registry (object storage), Mutation (via loadstring), Loop (REPL)
- **Verb System:** 31 verbs across 4 categories (sensory, inventory, object interaction, meta); tool resolution (capabilities-based, supports virtual tools like blood)
- **FSM Architecture:** Inline state machines for all objects; timer tracking (two-phase tick), room pause/resume, cyclic states
- **Containment:** 4-layer validation (identity, size, capacity, categories)
- **Composite Objects:** Single-file pattern with detachable parts; two-hand carry system
- **Skill System:** Binary table lookup; skill gates; crafting recipes on materials
- **GOAP Planner:** Tier 3 backward-chaining; prerequisite resolution; in-place container handling
- **Terminal UI:** Split-screen (status bar + scrollable output + input); pure Lua; ANSI support
- **Multi-Room Engine:** All rooms loaded at startup; shared registry; per-room FSM ticking

**Architectural Patterns (Foundational):**
- Objects use FSM states with sensory text; mutation is code-level only
- `engine/mutation/` is ONLY code that hot-swaps objects
- Tool resolution: capabilities (not tool IDs)
- Sensory verbs work in darkness
- Skills: double-dispatch gating (skill gate + tool gate)

## Learnings

- **Brass Key/Padlock Fix (Phase 4 walkthrough bug):** The `unlock` and `lock` verb handlers in `engine/verbs/containers.lua` were stubs — they found the target object but always printed "You can't unlock that" without attempting FSM transitions. Fixed by implementing full FSM transition logic (matching the `open`/`close` pattern) with `requires_tool` capability checks. Additionally, all three key objects (brass-key, iron-key, silver-key) lacked `provides_tool` fields, so `find_tool_in_inventory()` could never match them. Added `provides_tool = "{key-id}"` to each. Two-bug fix: engine verb handler + object metadata. Bidirectional portal sync already handled by `fsm.transition()` calling `sync_bidirectional()`.

- **Engine Code Review (2026-03-28):** Full Phase 1 senior review of all 68 engine files (~26K LOC). Identified 6 files for splitting: `verbs/helpers.lua` (1634→5 modules), `parser/preprocess.lua` (1282→6 modules), `verbs/sensory.lua` (1113→3 modules), `loop/init.lua` (624→4 modules, highest contention at 52 commits), `search/traverse.lua` (871→3 modules), `injuries/init.lua` (540→3 modules). Critical finding: `loop/init.lua` has zero test coverage despite being most-edited file. LLM sweet spot confirmed at 150-400 lines per module. Review deliverable: `docs/architecture/engine/refactoring-review-2026-03-28.md`. Key dependency: `verbs/helpers.lua` is required by all 17 verb modules — splitting it gives biggest context reduction. Sequencing: Nelson must write test baselines before any splitting begins.

- **Linter Improvement Implementation Plan (2026-07-29):** Wrote 6-wave, 5-gate implementation plan for meta-lint improvement across 3 phases. Key architecture decisions: (1) Multi-module structure (lint.py, config.py, rule_registry.py, cache.py, squad_routing.py) enables parallel agent work — different agents edit different modules in the same wave. (2) lint.py is the bottleneck file (~2,538 lines) — only one agent per wave can touch it, which drives wave serialization for bug fixes. (3) EXIT-* rules already implemented and registered (7/7) — WAVE-4 is verification, not greenfield. (4) CREATURE-* rules are the largest new work (0/20 exist) — ~150 lines of new validator code. (5) Fix-safety schema already exists in rule_registry.py (fixable, fix_safety fields) but only 5 of 164 rules have explicit classifications — WAVE-2 audit + WAVE-3 integration completes this. (6) No test/linter/ directory exists — biggest quality gap, addressed in WAVE-0. (7) Environment variants are a config extension, not a new module. Plan written in one pass (~51KB) rather than chunked — at the upper limit of single-pass but successful.

- **Worlds Implementation Plan — Phase 1 (2026-08-21):** Wrote 4-wave, 3-gate implementation plan for World-driven engine boot. Key architecture decisions: (1) New module `engine/world/init.lua` rather than inlining in main.lua — testable, aligns with refactoring direction (D-ENGINE-REFACTORING-WAVE2). (2) Dependency injection pattern: world module has zero `require()` calls; loader, read_file, and templates all passed as parameters. Same isolation pattern as engine/loader. (3) No hardcoded fallback — zero worlds = FATAL error, not silent degradation. Fail fast. (4) World NOT registered in object registry — it's metadata on `context.world`, not a game entity. (5) One-directional relationship: World → Level (no `world_id` on levels). (6) `list_lua_files` duplicated in world module rather than extracting shared utility — acceptable for Phase 1 scope; D-ENGINE-REFACTORING-WAVE2 handles extraction later. (7) Level path resolution uses `level_dir` parameter, not raw `file` field from world definition — avoids cross-platform path issues. (8) Plan written in one pass (~39KB), under the 40KB chunking threshold. Wave structure: WAVE-0 pre-flight, WAVE-1 loader+tests parallel, WAVE-2 boot integration+tests parallel, WAVE-3 docs+LLM verification. Only main.lua is modified among existing files; world module is purely additive.

- **Phase 3 Helpers Split (2026-08-21):** Split `engine/verbs/helpers.lua` into modular helpers (`helpers/core.lua`, `inventory.lua`, `search.lua`, `tools.lua`, `mutation.lua`, `combat.lua`, `portal.lua`) with a thin re-export layer to preserve the API. Maintained verb behavior via shared helpers and dependency injection, resolved alias conflicts (`carve`) by guarding butchery alias registration, and added `core.hobj` to share hand-resolution across modules. Test suite re-run: remaining failures match pre-existing baseline (silk-bandage healing, predator-prey, spider-web, room override).
- **Phase 3 Preprocess Split (2026-08-21):** Split `engine/parser/preprocess.lua` into submodules (`preprocess/data.lua`, `words.lua`, `core.lua`, `phrases.lua`, `compound_actions.lua`, `movement.lua`, `split.lua`) with a thin orchestrator preserving pipeline order and exported API. Removed cross-module cycles by having phrase transforms call `core.strip_articles`. Tests re-run: preprocess tests pass; full suite shows the same pre-existing failures (silk-bandage healing, predator-prey, spider-web, room override).
- **Phase 3 Sensory Split (2026-08-21):** Split `engine/verbs/sensory.lua` into `verbs/sensory/` modules (look, touch, search, smell, taste, listen) with a wrapper that registers handlers in the original order. Sensory tests pass; full suite retains the same baseline failures (silk-bandage healing, predator-prey, spider-web, room override).
- **Phase 3 Traverse Effects Split (2026-08-21):** Split `engine/traverse_effects.lua` into `engine/traverse_effects/registry.lua` (registry/process) and `engine/traverse_effects/effects.lua` (built-in handlers), with a wrapper that loads built-ins and preserves the original API. Full suite retains the same baseline failures (silk-bandage healing, predator-prey, spider-web, room override).

- **Issues #372 & #376 Verified Fixed (regression tests added):** Both bugs (butchery test crash and CLI --room crash) shared the same root cause: `require("engine.verbs.helpers")` failing before the Phase 3 split. The split (759ef41) created `helpers.lua` as a thin facade that re-exports from 7 submodules in `helpers/`. Added `test/engine/test-helpers-facade.lua` with 12 assertions covering facade loading, submodule loading, export completeness, and engine.verbs boot path. Full suite: 243/243 pass. Architectural note: the facade pattern uses `X.lua` alongside `X/` directory (not `X/init.lua`) — Lua resolves `X.lua` first, so this works but is non-standard. Future refactoring could rename facades to `init.lua` for convention compliance.

- **Issues #386, #382, #368 Fixed; #375 Verified Already Fixed (Wave 3 bug batch):** Four engine/misc bugs triaged. (1) **#386 — Linked exit sync:** FSM transitions on objects with `linked_exit` + `linked_passage_id` now sync the corresponding room exit's state via `becomes_exit` mutations. Added sync logic to both `destruction.lua` (break handler) and `containers.lua` (open handler) after successful FSM transitions. Pattern: object declares which exit direction and passage_id it's linked to; exit declares `mutations.{verb}.becomes_exit` with the target state properties. (2) **#382 — Burn no-flame message:** Updated `fire.lua` burn handler's no-flame error message to include "no flame" substring that the test asserts. The registry removal for consumed flammable objects was already working correctly in the generic destruction path. (3) **#375 — Level intro in headless:** Already fixed — the narrative print loop in `main.lua` has no headless guard. All 11 tests pass. (4) **#368 — Rename goto→teleport:** Swapped primary/alias in `movement.lua` so `teleport` is the primary handler registration and `goto` is the backward-compatible alias. Updated usage message. Full suite: 243/243 pass, zero regressions.

- **Issues #343, #308 Fixed; #319, #329 Verified Already Fixed (4-bug engine batch):** Four playtest-discovered engine bugs addressed. (1) **#343 — Butchery alive guard bypass:** The guard `if target.alive ~= false and not target.death_state then` evaluated to `false` for living wolves with `death_state`, letting players butcher living creatures. Fixed to standalone `if target.alive ~= false then` — rejects all living creatures regardless of `death_state`. Added test 5 to `test/butchery/test-butcher-verb.lua` with living wolf + `death_state` fixture. (2) **#308 — Rest verb missing cure_stress:** `rest.lua` had zero stress handling. Added `injuries.cure_stress(player, ctx, sleep_hours)` call after the sleep tick loop, gated by pcall module load, safe-room check (internal to `cure_stress`), and duration minimum. Added tests 3-4 to `test/stress/test-stress-cure.lua` exercising the rest verb handler directly. (3) **#319 — Wolf max_health lookup:** Already fixed — line 470 of `verbs/init.lua` correctly uses `creature.max_health` (not the buggy `creature.combat.max_health`). Existing test file `test/creatures/test-bug319-retreat-maxhealth.lua` covers the scenario. (4) **#329 — Use poultice injury guard:** Already fixed — lines 518-524 of `verbs/meta.lua` check `obj.cures` before allowing `use` on healing items. Poultice has top-level `cures` field. Existing tests in `test/verbs/test-p1-bugs-329-331-338-344-345-370-313.lua`. Full suite: 244/244 pass, zero regressions.

- **Issues #294, #330 Fixed; #303, #341, #324 Verified Already Fixed (5-bug engine batch):** Five engine bugs triaged. (1) **#294 — Duplicate-type take guard:** The take handler's duplicate check at `acquisition.lua:175-193` compared objects by `obj.id` (string equality), blocking players from holding two different instances of the same template. Fixed to use table identity comparison (`_hobj(hand, ctx.registry) == obj`) — only the exact same object (table reference) is blocked, different instances of the same type are allowed. Same fix for worn-item guard. 5 TDD tests at `test/inventory/test-bug294-duplicate-type.lua`. (2) **#330 — Standalone flee/run verb:** The `flee` handler existed at `init.lua:659` but `run` was aliased to `go` (movement). Fixed by overriding `handlers["run"]` and adding `handlers["escape"]` as flee aliases, registered after `movement.register()` so they take priority. Added 'away' noun stripping for 'run away'/'flee away'. Flee falls back to movement for direction nouns when no threats present. 9 TDD tests at `test/verbs/test-bug330-standalone-flee.lua`. Updated `test/verbs/test-parser-synonyms.lua` to reflect `run` → flee. (3) **#303 — Floor ingredients in crafting:** Already fixed — crafting code searches hands, bags, AND room floor. GUID-based duplicate-consumption guard. 4 tests pass. (4) **#341 — Spoilage FSM timing:** Already fixed — delays scaled to 3600/7200/14400 (rat) and 7200/10800/14400 (wolf), all exceeding SECONDS_PER_TICK (360). 6 tests pass. (5) **#324 — Wolf prey list missing player:** Already fixed — wolf.lua has "player" at prey position 1, predator-prey.lua checks context.player separately. 6 tests pass. Full suite: 247 files, 0 regressions (1 pre-existing in injuries-comprehensive). Commit: ff953a4.

- **Issue #317 Fixed; #315 Deferred as Design Limitation (2-bug engine batch):** (1) **#317 — Sub-threshold stress feedback:** `injuries.list()` showed "You feel fine" when stress was 1-2 (below "shaken" threshold of 3). Root cause: `has_stress` check required `get_stress_level(player) ~= nil`, which returns nil for sub-threshold stress. Fixed by splitting the stress display into two paths: at/above threshold shows the level description (e.g. "Your hands tremble slightly."), below threshold shows "You feel a growing unease." 5 TDD tests at `test/stress/test-stress-subthreshold.lua`. Commit: 016ddc0. (2) **#315 — Pack tactics untestable (1 wolf):** Investigated and found this is a design limitation, not an engine bug. `wolf.lua` already has `max_population = 3`, pack-tactics unit tests pass (alpha selection, stagger, retreat), respawn module correctly spawns additional wolves. The gap is content: only 1 wolf instance placed in `hallway.lua`. Suggested 3 options in issue comment (add instances, create wolf den room, or add `spawn_initial_population` engine feature). Deferred to Comic Book Guy for design direction. Full suite: 254/254 pass, zero regressions.

- **Issues #400, #397, #396, #395 Fixed (4-bug engine batch):** Four bugs from Nelson's test-pass audit and playtest. (1) **#395 — Burn handler ignores `obj.flammable`:** The burn handler in `fire.lua` derived burnability solely from `material.flammability`, but objects with explicit `flammable = true` and no material (e.g. paper) defaulted to flammability 0, hitting the `BURN_THRESHOLD` guard. Fixed by adding `obj.flammable` bypass before the threshold check. Also fixed the `light` handler's redirect-to-burn path. (2) **#396 — Unlock wrong-key message:** The `unlock` handler for exit doors in `containers.lua` printed generic "You don't have the right key" when the player held a wrong key. Tests expected "doesn't fit." Fixed by adding wrong-key detection: after failing to match `exit.key_id`, handler checks if player holds any key-like object and prints "The {name} doesn't fit." (3) **#397 — 4 test-verb-comprehensive failures:** Two `run → go` alias tests: updated to expect `run = flee` per #330 design decision. Two unlock/lock tests: same unlock fix as #396; lock handler had logic bug (`if key_obj or exit.key_id` always true when exit had key_id) — fixed to require actual key match; changed message to include "locked". (4) **#400 — Wolf wanders to deep-cellar and instant-kills player:** The wander action in `engine/creatures/actions.lua` had no territory constraint on exit selection. Wolf with `territory = "hallway"` could freely traverse open stairways. Fixed by adding territory leash: when creature has `behavior.territory`, valid wander exits are filtered to `behavior.patrol_rooms` (defaults to `{territory}`). Wolf stays in hallway; if displaced, returns home. Creatures without territory unaffected. 4 TDD tests at `test/creatures/test-bug400-wolf-territory-leash.lua`. Full suite: zero regressions beyond pre-existing failures.

- **Regenerated Lost Implementation Plans (2026-08-21):** Recovered two implementation plans that were created in a prior session but never committed. (1) **Worlds Implementation Plan** (`plans/worlds/worlds-implementation-phase1.md`, ~31KB): 4 waves, 3 gates for World-driven engine boot. Introduces `engine/world/init.lua` with dependency injection pattern (zero `require()` calls). World → Level → Room hierarchy. Single-world auto-select, zero-world FATAL. World 1 = "The Manor", `starting_room = "start-room"`. World is metadata on `context.world`, NOT registered in object registry. (2) **Linter Improvement Implementation Plan** (`plans/linter/linter-improvement-implementation-phase1.md`, ~44KB): 6 waves, 5 gates covering all 3 phases of linter improvement. lint.py bottleneck drives wave serialization (one editor per wave). Phase 1: bug fixes #190/#195/#196 + fix-safety audit + --fix CLI. Phase 2: EXIT-* verification + CREATURE-* greenfield (20 rules). Phase 3: environment variants, squad routing, incremental caching. Footer notes mutation graph linter is separate effort. Commit: 2e00946.

- **Test Speed Design Plan (2026-08-22):** Researched and wrote comprehensive design plan for faster unit test execution (`plans/testing/test-speed-design.md`, ~20KB). Key findings: 257 test files run serially in ~156s; single benchmark (`test-inverted-index.lua`) consumes 61.6s (40% of runtime) doing 100 iterations of embedding matching; tests are already process-isolated (safe for parallelism); `squad-ci.yml` exists but is unconfigured. Five approaches analyzed: (A) Benchmark gating — rename benchmarks to `bench-*`, skip by default, -66s instant win. (B) Parallel execution — 8-worker PowerShell/shell runner, 6× speedup, tests already isolated. (C) File splitting — 9 files >40KB but size doesn't correlate with runtime; low priority. (D) GitHub Actions CI — basic Lua CI setup, matrix sharding across 6 runners, auto-file GitHub issues on failure via `actions/github-script@v7` with duplicate detection and auto-close on success. (E) Incremental testing — git-diff-based source-to-test mapping with `--changed` flag. Recommended path to <20s dev feedback: benchmark gating + parallel runner (4 hours total work). CI auto-issue filing confirmed feasible with complete implementation spec. Commit: 03523df.

- **Test Speed Implementation Plan (2026-08-22):** Converted design plan into full 5-wave, 4-gate implementation plan (`plans/testing/test-speed-implementation-phase1.md`, ~55KB). Covers design phases 1-3 (benchmark gating, parallel runner, CI, auto-issues, matrix sharding, incremental testing, cross-platform). Phase 4 (file splitting) deferred to D-ENGINE-REFACTORING-WAVE2. Key architecture decisions: (1) Separate parallel runner (`run-tests-parallel.ps1`) — keeps `run-tests.lua` as serial fallback. (2) Nelson owns ALL `run-tests.lua` modifications across waves (single-editor-per-file rule). (3) Gil owns ALL CI workflow changes. (4) PowerShell 7 `ForEach-Object -Parallel` for Windows; `xargs -P` for Unix. (5) `--shard` flag uses directory-name matching with 6 named shards (parser, verbs, creatures, rooms, search, other) — `other` is catch-all for remaining dirs. (6) `--changed` flag uses conservative source-to-test mapping with "run all" fallback for unrecognized source files. (7) Three files maintain test directory lists (run-tests.lua, .ps1, .sh) — documented as sync requirement, future extraction to shared config deferred. (8) CI auto-issue filing uses per-shard issues with duplicate detection, not a single mega-issue. (9) `bench-*` naming convention over metadata/config approach — simple, grep-able, discoverable. (10) WAVE-0 explicitly audits `test-bm25-deep.lua` before renaming — might be correctness, not benchmark.

- **Linter WAVE-1 — XF-03 config infrastructure (2026-08-22):** Added per-rule configuration support to `scripts/meta-lint/config.py` for WAVE-1 parallel work. Three additions: (1) `DEFAULT_RULE_CONFIG` dict at module level with XF-03 defaults (`allowed_shared: ["match","key","door"]`, `cross_room_severity: "info"`). (2) Module-level `get_rule_config(rule_id, key, default=None)` for simple lookups against defaults. (3) `CheckConfig.rule_params` field + `CheckConfig.get_rule_config()` instance method that checks user overrides from `.meta-check.json` first, then falls through to defaults. `parse_config()` now extracts extra keys (beyond `enabled`/`severity`) from the rules JSON section into `rule_params`. Smithers can call either `config.get_rule_config("XF-03", "allowed_shared")` (module-level) or `_active_config.get_rule_config("XF-03", "allowed_shared")` (instance, respects user overrides). Design extensible — adding config for future rules is just another key in `DEFAULT_RULE_CONFIG`.

- **Mutation Graph Linter — Pivot to Expand-and-Lint (2026-08-23):** Wayne directed a fundamental pivot on the mutation graph linter approach. Original design: build a pure-Lua graph library with BFS/DFS, cycle detection, and 7 test suites (~350-400 LOC, ~240 tests) inside `test/meta/test-mutation-graph.lua`. New design: (1) Lua edge extractor script (`scripts/mutation-edge-check.lua`, ~100-150 LOC) that scans `src/meta/`, extracts mutation edges from 5 mechanisms, and verifies target file existence. (2) Pipe valid target file paths to existing Python meta-lint (`scripts/meta-lint/lint.py`) for full 200+ rule coverage. Key insight: composing two existing tools (Lua for loading Lua objects, Python for linting) is simpler AND more powerful than building a custom graph validator. Eliminated: graph data structure, BFS/DFS, cycle detection, unreachable node detection, 7 inline test suites. Added: `--targets-only` CLI flag for piping, `scripts/mutation-lint.ps1` wrapper. Implementation plan reduced from 4 waves/3 gates to 3 waves/2 gates. Estimated effort dropped from ~6 hours to ~4-5 hours. Architecture lesson: prefer composing existing tools over building new ones when the coverage is equivalent or better.

- **Mutation Graph Linter — Impl Plan Review (2026-08-23):** Reviewed implementation plan against design doc, engine codebase, and test runner. Found 3 blockers, all fixed: (1) `safe_load` sandbox specified Lua 5.1 `setfenv` only — we run Lua 5.4.6 which requires `loadfile(filepath, "t", env)`. Fixed both impl plan and design doc with version-branching pattern matching `engine/loader/init.lua` lines 70-76. (2) Sandbox env missing 5 builtins from engine's `make_sandbox()` — `next`, `select`, `unpack or table.unpack`, `error`, `pcall`. Any object using these would fail to load in extractor but work in-game. Fixed. (3) `mutation-lint.ps1` wrapper declared `$Env` parameter but never passed it to `lint.py --env`. Fixed. Also added `2>nul`/`2>/dev/null` error suppression note on `io.popen` calls (matching `test/run-tests.lua` pattern). Key files checked: `src/engine/loader/init.lua` (sandbox source of truth), `test/run-tests.lua` (cross-platform `io.popen` pattern), `scripts/meta-lint/lint.py` (CLI accepts single file paths via positional arg, confirmed at line 2378-2280).

- **Mutation Graph Linter — Full Team Review Incorporation (2026-08-23):** Updated impl plan (v1.0→v2.0) and design doc with all findings from 9-agent review. Major changes: (1) **Flanders: 19 invisible creature edges** — added 7 new extraction mechanisms (loot_table 4 sub-patterns, death_state.crafting, death_state.butchery_products, behavior.creates_object). LOC estimate increased to ~130-190. Recursive `death_state` pass added. (2) **Smithers: 2 blockers fixed** — `--json` deferred to WAVE-2 (no consumer), parallel output interleaving solved with per-file collection + sequential display + section headers. Also: `--targets-only` → `--targets`, ⚡→⚠, source file paths in error output, stderr format specified, success/failure footer added. (3) **Nelson: 12 test improvements** — file count threshold 80→150 (actual 206), 6 new test cases (non-table return, empty mutations, duplicate spawns, creature edges, subdirectory coverage), assert_gt workaround documented, source_to_tests mapping, shard assignment, exact gate verification commands, Python availability guards. (4) **Gil: 4 CI items** — PS7 fallback for -Parallel, squad-ci.yml Python+lint step, pre-deploy gate edge-check, .gitattributes line endings. (5) **CBG: routing + future work** — courtyard-kitchen-door routes to Moe, multi-hop chains deferred as D-MUTATION-CYCLES-V2. (6) **Brockman: 5 doc items** — Motivation section added to design doc, real-world example trace, README updates flagged for WAVE-2. (7) **Moe, Bob: all clear** — rooms have zero mutations (confirmed), 7 crafting chains covered (confirmed). Filed 2 decisions: D-MUTATION-LINT-PARALLEL, D-MUTATION-CYCLES-V2. Plan is now WAVE-0 ready with zero open questions. Architecture lesson: full-team review on plans before implementation catches integration issues (creature edge nesting, output interleaving, PS7 compatibility) that would be expensive to fix after code is written.

- **Cross-Agent Mutation Linter Review (2026-03-28):** Nine-agent review wave on mutation-graph-linter implementation plan. Cross-agent findings for Bart: (1) **Flanders creature edges gap:** 19 invisible mutation edges on creatures (wolf, spider, rat) that will trigger WAVE-1 broken-edge reports. These are puzzle-critical deferred features, not bugs — edges are intentional (creature progression mechanics). Will create target creature-state files post-WAVE-0 launch. (2) **Nelson test infrastructure gaps:** 12 test improvements identified + Python 3.9+ environment validation needed (CI currently defaults to 3.8 or 3.10). Nelson flags this; Bart updates WAVE-0 pre-flight script. (3) **Smithers blockers:** JSON schema for `--json` output undefined (Bart to spec before impl), parallel lint output will interleave (Smithers to recommend collection + sequential print). (4) **Gil CI gaps:** GitHub Actions missing Python 3.9 + no lint step — Bart adds `setup-python@v4` + `-Version 7.0` PowerShell guard to wrapper. Lessons: Mutation-edge taxonomy (5 types formalized by Bart) needed for extraction; cross-team artifact synchronization now tracked in decisions (D-MUTATION-LINT-PIVOT, D-PARALLEL-EXPAND-LINT, D-MUTATION-EDGE-EXTRACTION).


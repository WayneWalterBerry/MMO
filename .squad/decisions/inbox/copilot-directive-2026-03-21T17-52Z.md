### 2026-03-21T17:52Z: User directive
**By:** Wayne Berry (via Copilot)
**What:** Debug/loading messages (object names, file sizes, load times) must only display when `?debug` is present in the URL. Default player experience shows no loading details — they spoil discovery. Bug reports may still include debug metadata regardless of the flag.
**Why:** User request — showing "Loading oak-wardrobe..." reveals room contents before the player finds them.
